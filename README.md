# PDF RAG Assistant

A production-ready Retrieval-Augmented Generation (RAG) system that answers questions from PDFs.

## Features
- Multi-PDF upload
- Semantic search (FAISS)
- Local LLM (Ollama)
- FastAPI backend
- Persistent storage

## Run

pip install -r requirements.txt  
fastapi dev main.py
